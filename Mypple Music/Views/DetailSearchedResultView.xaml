<UserControl x:Class="Mypple_Music.Views.DetailSearchedResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Mypple_Music.Views"
             xmlns:control="clr-namespace:Mypple_Music.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Border BorderBrush="#616161"
                Margin="0,0.5,0,0"
                BorderThickness="0,0,0,0.3" />
        <TextBlock x:Name="Title"
                   Text="{Binding KeyWords}"
                   d:Text="艺人"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="10 0 0 0" />
        <StackPanel Grid.Row="1">
            <control:MusicDataGrid />
            <control:AlbumListBox />
        </StackPanel>
    </Grid>
</UserControl>
